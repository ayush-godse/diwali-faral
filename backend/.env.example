# MongoDB connection string (replace with your URI)
MONGO_URI=mongodb://localhost:27017/diwali-faral

# JWT secret for signing tokens
JWT_SECRET=your_jwt_secret_here

# Email account for sending order confirmations (Gmail example)
EMAIL_USER=youremail@gmail.com
EMAIL_PASS=your_app_password_or_smtp_password

# Optional: PORT
PORT=5000
